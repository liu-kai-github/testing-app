describe('全局 API', () => {

    beforeAll(() => {
        // 测试开始前
    });

    afterAll(() => {
        // 测试结束后
    });

    beforeEach(() => {
        // 每一项测试开始前
    });

    afterEach(() => {
        // 每一项测试结束后
    });

    it('it 案例', () => {
        expect('').toBe('');
    });
});
